import { useLoaderData } from "react-router-dom";

const Users = () => {

    const allData =useLoaderData();
    console.log(allData)

    return (
        <div>
            
        </div>
    );
};

export default Users;